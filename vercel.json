{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd preview && npm install && npm run build",
  "outputDirectory": "preview/dist",
  "installCommand": "echo skip",
  "framework": null,
  "functions": {
    "api/derive-config.js": {
      "maxDuration": 60
    }
  }
}
